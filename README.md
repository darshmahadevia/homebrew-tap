# Homebrew tap

Homebrew formulae for Darsh Mahadevia's projects.

## Limitr

Install the local-first Codex rate-limit viewer:

```sh
brew install darshmahadevia/tap/limitr
```

Limitr requires a compatible `codex` executable on `PATH`, but does not require
Rust or Cargo at runtime.
